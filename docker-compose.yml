services:
    bot:
        build: ./bot
        image: xyzzy-gpt-bot:latest
        restart: unless-stopped
        ports:
            - "8080:8080"
        volumes:
            - ./bot/db:/db
        environment:
            - DATABASE_URL=sqlite:///db/data.db
